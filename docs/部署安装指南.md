# ZCard 部署安装指南(含常见问题排查)

> 本文档汇总了 ZCard 在生产环境(尤其是宝塔面板 / LNMP 一键包等)部署时最常遇到的安装与运行问题,并给出标准解决方案。
>
> **强烈建议**在执行 `composer install` 之前,先按「[第 1 步:环境准备](#第-1-步环境准备关键)」一次性配好 PHP,绝大多数坑可以避免。

---

## 目录

- [环境要求速览](#环境要求速览)
- [第 1 步:环境准备(关键)](#第-1-步环境准备关键)
- [第 2 步:安装依赖](#第-2-步安装依赖)
- [第 3 步:完成安装](#第-3-步完成安装)
- [第 4 步:启动队列进程(必做,否则上游拿货/批量导入不工作)](#第-4-步启动队列进程必做否则上游拿货批量导入不工作)
- [第 4.5 步:配置计划任务 Cron(必做,否则超时关单/自动同步不生效)](#第-45-步配置计划任务-cron必做否则超时关单自动同步不生效)
- [常见问题排查(FAQ)](#常见问题排查faq)
  - [问题 1:Composer 满屏 Deprecated 警告 + `composer-runtime-api` 不匹配](#问题-1composer-满屏-deprecated-警告--composer-runtime-api-不匹配)
  - [问题 2:首页报 `Failed to open stream: vendor/autoload.php`](#问题-2首页报-failed-to-open-stream-vendorautoloadphp)
  - [问题 3:`ext-fileinfo is missing from your system`](#问题-3ext-fileinfo-is-missing-from-your-system)
  - [问题 4:`Call to undefined function putenv()`](#问题-4call-to-undefined-function-putenv)
  - [问题 5:访问首页 500 / 白屏(storage 目录不可写)](#问题-5访问首页-500--白屏storage-目录不可写)
  - [问题 6:`tempnam(): file created in the system's temporary directory`](#问题-6tempnam-file-created-in-the-systems-temporary-directory)
  - [问题 7:`Module "mbstring" is already loaded`](#问题-7module-mbstring-is-already-loaded)
  - [问题 8:货源接口报 404](#问题-8货源接口报-404supply-sourcesidproductspreview-could-not-be-found)
  - [问题 9:接口报错返回 HTML 404 页面而不是 JSON](#问题-9接口报错返回-html-404-页面而不是-jsonerror_page-404-404html-掩盖错误)
  - [问题 10:上传的图片 / `/storage/...` 访问 403「访问被拒绝」,图片不显示](#问题-10上传的图片--storage-访问-403访问被拒绝图片不显示)
  - [问题 11:`composer install` 报 `Your lock file does not contain a compatible set of packages` / `symfony/yaml requires php >=8.4.1`](#问题-11composer-install-报-your-lock-file-does-not-contain-a-compatible-set-of-packages--symfonyyaml-requires-php-841)
  - [问题 12:`composer install` 报 `The file "/tmp/.composer/config.json" is not readable`](#问题-12composer-install-报-the-file-tmpcomposerconfigjson-is-not-readable)
  - [问题 13:订单已支付但没拿到上游卡密(队列未启动 / 拿货任务堆积)](#问题-13订单已支付但没拿到上游卡密队列未启动--拿货任务堆积)
  - [问题 14:图片上传失败 / 验证码不显示 / 发起支付报错(扩展缺失或禁用函数)](#问题-14图片上传失败--验证码不显示--发起支付报错扩展缺失或禁用函数)
  - [问题 15:更新后后台/前台空白页(Cloudflare Rocket Loader)](#问题-15更新后后台前台空白页cloudflare-rocket-loader)
  - [问题 16:更新后接口报 `Unknown column ... in field list`(数据库迁移未执行)](#问题-16更新后接口报-unknown-column--in-field-list数据库迁移未执行)
- [错误速查表](#错误速查表)
- [部署后检查清单](#部署后检查清单)

---

## 环境要求速览

| 组件 | 版本要求 | 说明 |
|---|---|---|
| **PHP** | **8.3 或 8.4** | Laravel 13 要求,见下方扩展清单 |
| **MySQL** | **>= 8.0**(推荐 8.4) | utf8mb4 字符集 |
| **Redis** | **>= 6.0**(可选) | 不装可降级为 database 缓存 |
| **Composer** | **>= 2.8**(推荐最新 2.10.x) | **Laravel 13.8+ 要求 `composer-runtime-api ^2.2`,旧版会报错** |

### PHP 必需 / 推荐扩展

| 扩展 | 级别 | 用途 |
|---|---|---|
| `pdo_mysql` | **必需** | 数据库 |
| `mbstring` | **必需** | 多字节字符串(多语言) |
| `openssl` | **必需** | 加密 / HTTPS |
| `ctype` `tokenizer` `xml` `dom` `filter` `hash` | **必需** | Laravel 核心 |
| `curl` | **必需** | Stripe / 支付回调 / HTTP 请求 |
| `fileinfo` | **必需** | 文件上传 / MIME 检测 **(宝塔常缺,见问题 3)** |
| `bcmath` | **必需** | **金额精确计算(项目以「分」整数记账,缺了会出精度问题)** |
| `gd` 或 `imagick` | **必需** | 图片处理 / 图形验证码(`mews/captcha`) |
| `zip` | 推荐 | 压缩 / 导出 |
| `redis` | 可选 | 缓存加速 |

### 宝塔禁用函数(必须放行)

宝塔默认会禁用一批函数;部分宝塔「安全防护」插件还会额外禁用 `curl_exec` 等。
**被禁用的函数若恰好是 ZCard 依赖的,对应功能会静默失败**(如发起支付报错、
上游拉不到货、图片上传失败)。按功能分组如下:

| 函数 | 影响的功能 | 级别 |
|---|---|---|
| `putenv` | **Laravel 读 `.env`(Dotenv)、Composer、队列进程** | **必需** — 缺失直接崩溃(见问题 4) |
| `curl_exec` `curl_multi_exec` | **支付发起(支付宝原生驱动)、货源对接(上游 HTTP)、易支付/聚合支付回调**、远程图片抓取 | **必需** — 被安全插件禁用时「发起支付报错 / 上游拿不到货 / 回调不生效」 |
| `proc_open` | **后台「在线更新」**(UpdateController 执行 git/composer/pnpm)、队列进程 | 用在线更新则必需(见问题 8) |
| `shell_exec` `exec` `system` `passthru` `popen` | 在线更新的命令执行(UpdateController 备选路径) | 用在线更新则建议放行 |

> **图片上传 / 图形验证码不依赖上述禁用函数**,而是依赖 **PHP 扩展** `gd` 与 `fileinfo`
> (见上表「必需扩展」;上传图片失败或验证码不显示,先查扩展再查禁用函数,见问题 14)。

> 在宝塔面板 **「PHP 设置 → 禁用函数」** 中,把这些函数从列表里删除即可;
> 若装了宝塔「安全防护」等插件,还需在其配置里放行 `curl_exec`。

---

## 第 1 步:环境准备(关键)

> 以下以 **宝塔面板 + PHP 8.4** 为例。其他环境思路相同(改 php.ini / 用对应扩展安装方式)。
> 路径中的 `/www/server/php/84/` 是宝塔 PHP 8.4 的默认安装位置,若用 PHP 8.3 请改为 `/www/server/php/83/`。

### 1.1 确认 PHP 版本与扩展

```bash
# PHP 版本应为 8.3 或 8.4
/www/server/php/84/bin/php -v

# 列出已装扩展(逐一核对上面清单)
/www/server/php/84/bin/php -m
```

**缺扩展时,在宝塔面板安装:**

1. 软件商店 → 找到 **PHP-8.4** → 点「设置」
2. 选「安装扩展」标签
3. 找到缺失的扩展(尤其 **fileinfo、bcmath、gd、zip**)→ 点「安装」
4. 等待编译完成(约 1–2 分钟 / 个)

### 1.2 放行被禁用的函数

1. 宝塔面板 → **PHP-8.4 → 设置 → 禁用函数**
2. 在列表中找到并 **删除**:`putenv`(必须)、`proc_open`(推荐)
3. 保存

**或命令行改 ini**(php.ini 与 php-cli.ini 两个文件都要改):

```bash
# 查看当前禁用列表
grep -i "disable_functions" /www/server/php/84/etc/php.ini
grep -i "disable_functions" /www/server/php/84/etc/php-cli.ini

# 移除 putenv / proc_open
sed -i 's/, *putenv//; s/putenv, *//; s/putenv//; s/, *proc_open//; s/proc_open, *//; s/proc_open//' \
    /www/server/php/84/etc/php.ini /www/server/php/84/etc/php-cli.ini

# 重载 PHP-FPM
/etc/init.d/php-fpm-84 reload 2>/dev/null || systemctl reload php-fpm-84 2>/dev/null
```

验证:

```bash
/www/server/php/84/bin/php -r "var_dump(function_exists('putenv'));"   # 应输出 bool(true)
```

### 1.3 升级 Composer 到最新版

Laravel 13.8+ 要求 `composer-runtime-api ^2.2`(即 Composer ≥ 2.8)。宝塔自带或系统 `composer` 可能是 2.0/2.1 老版本,**必须升级**,否则会在 `composer update/install` 时报满屏 Deprecated 警告并最终解析失败。

```bash
# 用项目对应的 PHP 下载最新 composer.phar
cd /tmp
/www/server/php/84/bin/php -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"
/www/server/php/84/bin/php composer-setup.php
/www/server/php/84/bin/php -r "unlink('composer-setup.php');"

# 安装到统一位置
mv composer.phar /usr/local/bin/composer
chmod +x /usr/local/bin/composer

# 验证:应显示 2.8.x 及以上、runtime-api 2.2
/www/server/php/84/bin/php /usr/local/bin/composer --version
```

> 💡 **建议**做软链,让 `php` / `composer` 命令默认指向正确版本,避免后续 `artisan` 命令用到错误的 PHP:
>
> ```bash
> ln -sf /www/server/php/84/bin/php /usr/local/bin/php
> ```

---

## 第 2 步:安装依赖

进入项目目录,使用 **`install`**(严格按锁文件 `composer.lock` 安装),**不要用 `update`**。

```bash
cd <你的项目目录>

# 确认锁文件已部署到服务器(很关键)
ls -la composer.lock    # 应存在,大小约 400KB+

# 生产环境安装:去掉 dev 依赖 + 优化自动加载
/www/server/php/84/bin/php /usr/local/bin/composer install --no-dev --optimize-autoloader
```

> ⚠️ **为什么必须用 `install` 而不是 `update`?**
> - `install` 严格按 `composer.lock` 安装,版本与开发者测试一致;
> - `update` 会重新解析所有包版本,可能拉到不兼容的新版本,线上容易出问题;
> - 若服务器没有 `composer.lock`,`install` 会退化成 `update` 行为 —— 所以务必确认锁文件随代码一起部署。

---

## 第 3 步:完成安装

依赖装好后,按 [README](../README.md) 的「安装方式」二选一:

```bash
# 命令行安装(运维人员)
cp .env.example .env
php artisan key:generate
php artisan zcard:install            # 交互式:提示输入数据库 + 管理员
# 或跳过数据库交互:
php artisan zcard:install --skip-db --email=admin@example.com --password=yourpassword
```

或浏览器访问 `http://你的域名/install` 走 Web 安装向导。

安装完成后:

```bash
# 修正 vendor 属主(宝塔 PHP-FPM 用户通常为 www)
chown -R www:www vendor bootstrap/cache storage

# 访问验证
# http://你的域名/        → 前台商城
# http://你的域名/admin/   → 后台管理
```

---

## 第 4 步:启动队列进程(必做,否则上游拿货/批量导入不工作)

**背景**:系统 `QUEUE_CONNECTION=database`,异步任务存在数据库 `jobs` 表中,
必须有一个常驻的 `queue:work` 进程消费它们。**不启动队列 = 以下功能静默失效**:

- **上游货源拿货**(最常见):顾客下单支付后,拿货任务 `FetchFromUpstream`
  由队列消费。货源为 `async` 模式、或同步拿货失败自动转异步时**必依赖队列**。
  队列不运行 = **订单已支付但永远拿不到上游卡密**(订单显示"已支付",无发货)。
- **大批量卡密导入**(>5000 条走 `ImportCardsJob`)。
- 其他异步任务。

**判断是否已启动**:

```bash
ps aux | grep "queue:work\|queue:listen"
# 有输出 = 已运行,可跳过本节;无输出 = 必须启动 ↓
```

### 方式 A:宝塔面板(推荐)

1. 软件商店 → 安装「Supervisor 管理器」。
2. 添加守护进程:
   - **启动命令**: `php artisan queue:work --tries=3 --timeout=300`
   - **运行目录**: 项目根目录(如 `/www/wwwroot/zcard`)
   - **进程数量**: `2`(按需)
3. 启动后确认状态为「运行中」。

### 方式 B:命令行(supervisor)

创建 `/etc/supervisor/conf.d/zcard-queue.conf`:

```ini
[program:zcard-queue]
process_name=%(program_name)s_%(process_num)02d
command=php /www/wwwroot/zcard/artisan queue:work --tries=3 --timeout=300
autostart=true
autorestart=true
user=www
numprocs=2
redirect_stderr=true
stdout_logfile=/www/wwwroot/zcard/storage/logs/queue.log
```

```bash
supervisorctl reread && supervisorctl update && supervisorctl start zcard-queue:*
```

### 方式 C:临时启动(测试用,重启服务器后失效)

```bash
nohup php artisan queue:work --tries=3 --timeout=300 > storage/logs/queue.log 2>&1 &
```

**验证队列工作**:

```bash
ps aux | grep queue:work                      # 有进程
php artisan tinker --execute="echo DB::table('jobs')->count();"   # 正常应为 0 或很小
# 如果 jobs 表长期堆积(尤其 FetchFromUpstream),说明队列没在消费
```

---

## 第 4.5 步:配置计划任务 Cron(必做,否则超时关单/自动同步不生效)

**背景**:以下功能由 **Laravel Scheduler** 驱动,依赖服务器每分钟执行
`php artisan schedule:run`:

- **订单超时关闭**:每 5 分钟扫描并关闭超时未支付订单(后台「订单超时关闭(分钟)」
  设置生效的前提)—— **不配 cron = 订单永远不会超时关闭,前台一直显示"待支付"**。
- **分站利润解冻**、**货源商品自动同步**(每小时增量)、**nonce 清理**。

**配置**(服务器 crontab,一行):

```bash
crontab -e
# 添加(宝塔用户:计划任务 → Shell 脚本,内容同样):
* * * * * cd /www/wwwroot/zcard && php artisan schedule:run >> /dev/null 2>&1
```

**宝塔面板操作**:计划任务 → 添加任务 → Shell 脚本 →
任务名称 `zcard-schedule`,执行周期「每分钟」,
脚本内容 `cd /www/wwwroot/zcard && php artisan schedule:run >> /dev/null 2>&1`。

**验证**:

```bash
# 手动触发一次关单扫描,确认命令可执行
php artisan orders:close-expired
# 期望输出:已关闭 N 个超时订单
```

> ⚠️ 与队列(第 4 步)是**两个独立必配项**:队列管异步任务(拿货/导入),
> Cron 管定时任务(超时关单/自动同步),两者都要配。

---

## 常见问题排查(FAQ)

### 问题 1:Composer 满屏 Deprecated 警告 + `composer-runtime-api` 不匹配

**现象**

执行 `composer update` / `install` 时:

- 满屏 `Implicitly marking parameter $xxx as nullable is deprecated`
- 最后报错:
  ```
  laravel/framework[v13.8.0...] require composer-runtime-api ^2.2
  -> found composer-runtime-api[2.0.0] but it does not match the constraint.
  ```

**原因**

服务器上的 Composer 太旧(2.0 / 2.1 时代),其代码在 PHP 8.3/8.4 下触发大量 deprecation;且不提供 Laravel 13.8+ 要求的 `composer-runtime-api 2.2`。

**解决**

升级 Composer 到最新版,见 [1.3 升级 Composer](#13-升级-composer-到最新版)。

---

### 问题 2:首页报 `Failed to open stream: vendor/autoload.php`

**现象**

访问首页报:

```
Warning: require(.../public/../vendor/autoload.php): Failed to open stream:
No such file or directory in .../public/index.php on line 14
```

**原因**

`vendor/` 目录不存在 —— 依赖根本没装成功。这通常是 [问题 1](#问题-1composer-满屏-deprecated-警告--composer-runtime-api-不匹配) / [问题 3](#问题-3ext-fileinfo-is-missing-from-your-system) / [问题 4](#问题-4call-to-undefined-function-putenv) 任一导致 `composer install` 中断后的**表象**,不是独立 bug。

**解决**

先按其它问题让 `composer install` 成功跑完,`vendor/autoload.php` 自然生成。验证:

```bash
ls -la vendor/autoload.php    # 应存在
```

---

### 问题 3:`ext-fileinfo is missing from your system`

**现象**

`composer install` 时报:

```
league/flysystem-local 3.31.0 requires ext-fileinfo * -> it is missing from your system.
To enable extensions, verify that they are enabled in your .ini files:
    - /www/server/php/84/etc/php-cli.ini
```

**原因**

PHP 缺少 `fileinfo` 扩展。宝塔部分精简安装默认不带它。

**解决**(推荐真正装上,不要跳过)

在宝塔面板安装:

1. 软件商店 → **PHP-8.4 → 设置 → 安装扩展**
2. 找到 **fileinfo** → 安装

装完验证:

```bash
/www/server/php/84/bin/php -m | grep fileinfo    # 应输出 fileinfo
```

然后重新 `composer install`。

> 🚫 **不要**长期用 `--ignore-platform-req=ext-fileinfo` 绕过 —— 它只跳过依赖检查,**应用运行时一旦用到文件上传 / MIME 检测(商品图、头像等)就会报错**。

---

### 问题 4:`Call to undefined function putenv()`

**现象**

执行 `composer install` 直接崩:

```
PHP Fatal error: Uncaught Error: Call to undefined function
Composer\XdebugHandler\putenv() in .../xdebug-handler/src/Process.php:98
```

或一个隐蔽表现:**用 `/www/server/php/84/bin/php composer --version` 能跑,直接敲 `composer` 却崩** —— 因为两者加载的 `php.ini` 不同,直接敲时被 `disable_functions` 拦截。

**原因**

`putenv` 被宝塔加入 `disable_functions`。这不只影响 Composer —— **Laravel 读 `.env`(Dotenv)、队列进程都依赖它**,不放开会导致应用读不到环境变量。

**解决**

从禁用函数列表移除 `putenv`,见 [1.2 放行被禁用的函数](#12-放行被禁用的函数)。

---

### 问题 5:访问首页 500 / 白屏(storage 目录不可写)

**现象**

`composer install` 和 `git pull` 都成功,但访问首页 **500** 或白屏,日志里看不到清晰错误(或 Ignition 报各种框架内部错误)。

**原因**

`composer install` / `git pull` 用 **root** 执行,新生成的 `storage/`、`bootstrap/cache/` 目录属主是 root。而宝塔的 PHP-FPM 以 **www** 用户运行 → 这些目录**读得到但写不了** → Laravel 写 session / 日志 / 编译视图时崩溃 → 500。

这是**最常见的部署后 500 根因**,且非常隐蔽(CLI 命令以 root 跑都正常,只有 Web 请求才崩)。

**解决**

```bash
cd <你的项目目录>

# 把整个项目关键目录属主改回 www(宝塔 PHP-FPM 用户)
chown -R www:www storage bootstrap/cache vendor

# 确保目录可写
chmod -R 775 storage bootstrap/cache

# 重启 PHP-FPM(清 opcache)
/etc/init.d/php-fpm-84 restart
```

> 💡 **验证**:
> ```bash
> sudo -u www test -w <项目目录>/storage && echo "✅ www 可写" || echo "❌ 不可写"
> ```
> 从 v1.1.5 起,Web 安装向导和 `zcard:install` 命令会**自动尝试 chmod 修复**关键目录;但属主(chown)仍需手动执行(因为 PHP-FPM 进程无 chown 权限)。

---

### 问题 6:`tempnam(): file created in the system's temporary directory`

**现象**

访问首页 500,Ignition 错误页标题是 `tempnam(): file created in the system's temporary directory`。

**原因**

宝塔站点级 `.user.ini` 设置了 `open_basedir` 限制,把 PHP 能访问的路径锁在网站目录 + `/tmp`。当框架需要写的临时目录**不在允许列表**时触发此警告。

**排查**

```bash
# 查看 .user.ini(open_basedir 在这里设,不在 php.ini)
cat <项目父目录>/.user.ini

# 典型内容(注意路径层级):
# open_basedir=<项目父目录>/:/tmp/    ← 正确(包含 ZCard 父目录)
```

**解决**

确保 `open_basedir` 包含项目根目录和 `/tmp/`。在宝塔面板:**网站 → 你的站点 → 设置 → 配置修改**,把 `open_basedir` 改为:

```
open_basedir=/www/wwwroot/你的域名/:/tmp/
```

> ⚠️ 路径要用**项目父目录**,不要用项目目录本身,否则 Laravel 访问 `/tmp` 等系统路径会被拦。
>
> 如果排查后仍不确定,可临时**关闭防跨站攻击**(宝塔面板「网站设置 → 防跨站攻击 open_basedir」开关),验证是否为此问题。

---

### 问题 7:`Module "mbstring" is already loaded`

**现象**

每次执行 PHP 命令都报 `PHP Warning: Module "mbstring" is already loaded in Unknown on line 0`。

**原因**

`mbstring` 扩展被加载了两次:既编译进 PHP,又在 `php.ini` 里写了 `extension=mbstring`。

**解决**

```bash
# 找到重复的 extension 行(php.ini 和 php-cli.ini 都查)
grep -rn "extension.*mbstring" /www/server/php/84/etc/php.ini /www/server/php/84/etc/php-cli.ini

# 删除重复的那一行(保留一个,或都删掉如果已编译进 PHP)
# 例如:
sed -i '/extension=mbstring/d' /www/server/php/84/etc/php.ini
sed -i '/extension=mbstring/d' /www/server/php/84/etc/php-cli.ini

# 重启 PHP-FPM
/etc/init.d/php-fpm-84 restart
```

> 这只是 Warning,不影响功能,但建议清理避免日志噪音。

---

### 问题 8:货源接口报 404(`supply-sources/{id}/products/preview` could not be found)

**现象**

- 后台「货源对接 → 商品预览 / 勾选导入」无法使用
- 直接请求 `GET /api/admin/supply-sources/3/products/preview` 返回 404
- 报错可能是 Laravel JSON:`The route api/admin/supply-sources/3/products/preview could not be found`
  (也可能被 nginx 换成了 HTML 404 页面,见问题 9)

**原因**(按可能性排序)

1. **线上代码版本 < v1.9.2**:`products/preview`、`products/import` 路由是 **v1.9.2** 加入的,
   `products/debug` 是 **v1.9.5** 加入的。代码旧 → 路由不存在 → 404。
2. **git pull 未成功 / 工作区混乱**:手动 `git pull` 时本地有未提交改动(常见是
   `sysadmin/src/locales/langs/zh.json` 等被改过)导致 **Abort**,代码停留在旧版。
3. **PHP-FPM OPcache 缓存旧代码**:代码更新后未重启 PHP-FPM,`routes/api.php` 仍执行旧的
   字节码(此时 `route:clear` 无效,因为清的是 Laravel 缓存不是 PHP 字节码缓存)。
4. **货源 id 不存在**:`supply-sources/{id}` 的 id 在数据库中没有记录。
   (v1.9.6+ 已优化:会返回带「可用货源 id」列表的友好 JSON,便于定位)

**解决**(按顺序)

```bash
cd <你的项目目录>

# ① 确认版本与路由是否在源码里
cat VERSION                                 # 应 ≥ 1.9.2
grep -n "products/preview" routes/api.php   # 应能搜到,搜不到 = 代码旧

# ② 代码旧 → 强制同步到 GitHub 最新(⚠️ 会丢弃本地未提交改动)
git fetch origin main
git stash                                   # 想保留本地改动就先备份(可选)
git reset --hard origin/main
git stash pop                               # 恢复本地改动,可能有冲突(可选)

# ③ 清缓存 + 重启 PHP-FPM(清 OPcache,关键!)
php artisan route:clear && php artisan config:clear
/etc/init.d/php-fpm-84 restart

# ④ 验证路由已注册
php artisan route:list --path=supply-sources

# ⑤ 确认货源 id 存在(不是 404 的另一原因)
php artisan tinker --execute='print_r(App\Models\SupplySource::all(["id","name"])->toArray())'
```

**预防**

- **优先用后台「在线更新」按钮**,它会自动 `git pull` + 清缓存 + 迁移 + 重建前端,不会踩手动更新
  的坑;**不建议手动 `git pull`**。
- 手动 pull 前先 `git status` 检查本地改动,有改动先 `git stash`。
- 更新代码后**必须** `php artisan route:clear && php artisan config:clear` 并**重启 PHP-FPM**
  (否则 OPcache 可能继续跑旧代码,路由不生效)。

---

### 问题 8.5:更新后前台/后台仍显示旧界面(设置页缺「帮助中心」等新功能)

**现象**:代码已是新版(`cat VERSION` 正常),但浏览器打开仍是旧界面——
比如后台「页脚配置」没有「帮助中心/填入示例」、菜单没有新功能入口。

**原因**:浏览器或 Nginx **缓存了旧的 `index.html` 与旧 JS chunk**。
`index.html` 若被缓存,仍引用旧版本的 JS 文件;旧 JS 文件若也被缓存,
就一直是旧界面(v1.12.26 起产物已在 `index.html` 内注入 no-cache meta,
但**此前缓存的旧文件需要清一次才失效**)。

**解决**(按顺序)

```bash
# ① 浏览器强刷(最优先,90% 情况一步解决)
#    Windows/Linux: Ctrl + Shift + R     Mac: Cmd + Shift + R
#    或在浏览器设置里「清除浏览数据 → 缓存的图片和文件」

# ② Nginx/宝塔缓存清理(如果用了 nginx 静态缓存/CDN)
#    宝塔:软件商店 → Nginx → 性能调整 → 清缓存
#    或手动删除缓存目录后 reload
nginx -s reload

# ③ 长期方案:给 SPA 入口加 no-cache 响应头(推荐配置)
#    v1.12.26+ 产物已在 index.html 内嵌 no-cache meta,任何部署方式都生效;
#    如需 Nginx 层双保险,在 server 块加:
#    location = / { add_header Cache-Control "no-cache, no-store, must-revalidate"; }
#    location = /admin { add_header Cache-Control "no-cache, no-store, must-revalidate"; }
```

验证方法:更新后打开后台按 `F12` → Network → 刷新,看 `index.html` 与 JS 文件是否
从服务器重新加载(而非 `from disk cache`)。

---

### 问题 9:接口报错返回 HTML 404 页面而不是 JSON(`error_page 404 /404.html` 掩盖错误)

**现象**

- 调用 `/api/...` 接口报错时,返回的是 nginx 风格的 HTML 404 页面
  (页面标题 `404 Not Found`、带 `<center>nginx</center>` 或站点 `/404.html` 内容),
  而不是 JSON,导致无法判断是「路由不存在 / 资源不存在 / 权限不足」等真实原因。
- 典型场景:货源接口 404、后台列表接口 404,看到的全是同一个 HTML 页面。

**原因**

宝塔新建站点的默认配置里有一行:

```nginx
error_page 404 /404.html;
```

当 Laravel 返回 **404 状态码**(路由不存在、模型不存在等,响应体是 JSON)时,
nginx 会用 `/404.html` 的 **HTML 内容替换掉 JSON 响应体**。API-First 系统的
404 JSON(包括 v1.9.6 加的「货源不存在 + 可用 id」友好提示)全都会被吞掉。

**解决**

宝塔面板 → 网站 → 你的域名 → 设置 → **配置文件**,注释掉该行:

```nginx
#error_page 404 /404.html;
```

或让 API 路径保持 JSON、仅前端页面走错误页:

```nginx
location ~ ^/api/ {
    error_page 404 = @api404;
}
location @api404 {
    return 404;
}
```

改完:

```bash
nginx -t && nginx -s reload
```

> 该配置属于部署环境默认值,不是 ZCard 代码问题;注释掉后 API 的 JSON 错误就能正常透传,
> 排查问题会容易很多。

---

### 问题 10:上传的图片 / `/storage/...` 访问 403「访问被拒绝」,图片不显示

**现象**

- 素材管理 / 商品图上传成功后,图片 URL(`https://域名/storage/media/2026/08/xxx.png`)访问返回 **403 或 404**
- 页面显示「访问被拒绝」,图片裂图 / 无法预览
- 但后台列表能看到图片记录、能删除(文件其实已写入磁盘)

**原因**

素材图片存储在 `storage/app/public/media/`,必须通过 **`public/storage` 软链接**才能经 `/storage/...` 访问
(这是 Laravel 标准 `php artisan storage:link` 创建的结构)。若该软链接缺失或指向错误:

- 图片请求不会命中 nginx 的静态文件服务,而是被转发到 Laravel(走 `index.php`)
- Laravel 对 `/storage/` 没有对应路由 → 返回 **403「访问被拒绝」**(或 404)

> 排查关键:对比 `/admin/index.html`、`/storefront/index.html` 是否 200 正常、唯独 `/storage/...` 403。
> 若 `admin` / `storefront` 正常而 `storage` 异常,几乎可以断定是软链接问题。

**解决**

```bash
cd <你的项目目录>

# ① 检查软链接是否存在及指向
ls -la public/storage
# 期望:public/storage -> ../storage/app/public

# ② 不存在或指向错误 → 重建(Laravel 官方命令)
rm -f public/storage
php artisan storage:link

# ③ 验证图片可访问
curl -I https://你的域名/storage/media/2026/08/某图片.png   # 期望 HTTP/2 200
```

Docker 环境(如 Laravel Sail / 宝塔 Docker):

```bash
docker exec -it <你的容器名> bash
cd /www/sites/zcard/index        # 容器内项目路径
php artisan storage:link
```

**预防**

- v1.11.11 起,**安装流程会自动创建该软链接**(`InstallCommand` 与 Web 安装向导均已在迁移后执行 `storage:link`)。
- 升级旧版本站点后,请手动执行一次 `php artisan storage:link`。
- `public/storage` 是运行时产物(已被 .gitignore 忽略),**不会随 git 同步**,每台服务器都要单独创建。

---

### 问题 11:`composer install` 报 `Your lock file does not contain a compatible set of packages` / `symfony/yaml requires php >=8.4.1`

**现象**

- 服务器 `composer install` 报错,要求先 `composer update`
- 常见报错如:
  ```
  Problem: symfony/yaml v8.1.2 requires php >=8.4.1 -> your php version (8.3.0) does not satisfy that requirement.
  Problem: yansongda/pay v3.7.20 requires ext-bcmath * -> it is missing from your system.
  ```

**原因(分两类)**

1. **服务器 composer.lock 是旧版**:代码更新后(`git pull` / 在线更新),服务器上的 `composer.lock`
   没同步(可能是 git 冲突、或手动 pull 未成功),旧的 lock 里锁定了要求 PHP 8.4 的
   `symfony/* v8.1.x` 依赖,而服务器是 PHP 8.3 → 依赖校验失败。
2. **缺 `ext-bcmath` 扩展**:`yansongda/pay`(易支付等支付通道)、`dragon-code/support` 强制要求
   `bcmath` 扩展,服务器 PHP 未安装 → composer 校验失败。

**解决(按顺序)**

```bash
cd <你的项目目录>

# ① 先确认代码/ lock 已同步到最新(重要!)
git status
git log --oneline -1                  # 应 ≥ v1.11.3 的提交(该版本已将 Symfony 锁定到 7.4,兼容 PHP 8.3)
grep -A1 '"name": "symfony/yaml"' composer.lock | grep version   # 应为 v7.4.x,而不是 v8.x

# ② lock 旧 → 强制同步
git fetch origin main
git reset --hard origin/main          # ⚠️ 会丢弃本地未提交改动,先 git stash 备份

# ③ 安装 bcmath 扩展
# 宝塔:软件商店 → PHP → 设置 → 安装扩展 → 勾选 bcmath → 安装
# Docker 官方镜像:Dockerfile 加 RUN docker-php-ext-install bcmath 后重建
#   (容器内应急:docker exec -it <容器> bash && docker-php-ext-install bcmath)

# ④ 重装依赖
rm -rf /tmp/.composer                 # 清掉 Composer 全局配置缓存(权限问题,见问题 12)
composer install

# ⑤ 验证
php artisan --version
```

**预防**

- **优先用后台「在线更新」按钮**,不要手动 `git pull`。
- v1.11.3 起 `composer.json` 已:
  - 将 17 个 `symfony/*` 组件锁定 `^7.4`(仅要求 PHP 8.2,最低 PHP 真正降到 8.3)
  - 设置 `config.platform.php: 8.3.0`,防止 `composer update` 把依赖拉回要求 PHP 8.4 的版本
- `bcmath` 是**服务器扩展**,必须在 PHP 环境层面安装,代码无法替代。

---

### 问题 12:`composer install` 报 `The file "/tmp/.composer/config.json" is not readable`

**现象**

服务器 `composer install` 报:
```
In JsonFile.php line 205:
The file "/tmp/.composer/config.json" is not readable.
```

**原因**

Composer 默认把全局配置写到 `$COMPOSER_HOME`(未设置时通常是 `/tmp/.composer` 或 `~/.composer`)。
该文件可能由其他用户(如 root)创建,运行用户(如 `www-data`)无读权限 → 报不可读。

**解决**

```bash
# 方式一:删除损坏的全局配置,让 Composer 重新生成
rm -rf /tmp/.composer
composer install

# 方式二:显式指定 COMPOSER_HOME 到项目内目录(最稳)
mkdir -p /www/sites/zcard/index/.composer
chown -R www-data:www-data /www/sites/zcard/index/.composer
export COMPOSER_HOME=/www/sites/zcard/index/.composer
composer install
```

**预防**

- v1.11.4 起 `composer.json` 已设置 `config.home` / `config.cache-dir` 指向项目根目录的 `.composer/`,
  Composer 会在项目内读写配置与缓存,绕开系统 `/tmp` 的权限冲突。升级后按上面方式重新 install 即可。
- `.composer/` 已加入 `.gitignore`,不会进仓库。

---

### 问题 13:订单已支付但没拿到上游卡密(队列未启动 / 拿货任务堆积)

**现象**

顾客下单并支付成功,订单状态为「已支付」,但一直没有发货卡密;
后台订单管理里该单 `delivery_status` 长期为「待发货」或「发货失败」。

**原因**(最常见)

**队列进程未运行**。上游拿货走异步任务(`FetchFromUpstream`):
货源为 `async` 模式、或同步拿货失败自动转异步时,任务写入数据库 `jobs` 表,
必须由 `php artisan queue:work` 消费。队列不跑 = 拿货任务永远堆积,订单永远等不到卡密。

**排查**

```bash
ps aux | grep "queue:work\|queue:listen"    # 无输出 = 队列没跑
php artisan tinker --execute="echo DB::table('jobs')->count();"        # >0 = 任务堆积
php artisan tinker --execute="echo DB::table('failed_jobs')->count();" # >0 = 有失败任务
grep -E "supply|upstream" storage/logs/laravel.log | tail -20           # 拿货错误日志
```

**解决**

1. 启动队列(见上文「第 4 步」),堆积的拿货任务会自动消费并补发卡密。
2. 若 `failed_jobs` 有记录,查看失败原因(常见:上游网络不通 / 签名错误 / 商品编码错误)。
3. 拿货重试 5 次(10s/30s/1min/5min/15min)用尽后订单标记「发货失败」,
   需按货源配置的 `failure_action`(人工/自动退款)处理。

---

### 问题 14:图片上传失败 / 验证码不显示 / 发起支付报错(扩展缺失或禁用函数)

**现象**(常见于新部署的宝塔环境):

- 后台/前台**上传图片失败**(商品图、Logo、素材库),或上传后图片不显示;
- **图形验证码不显示**(登录/下单/注册验证码位置空白或裂图);
- **发起支付报错**(如支付宝「网关请求失败」)、**货源同步失败**;
- 图片尺寸信息缺失(素材库宽度/高度为空)。

**原因**(按可能性排序):

1. **缺 PHP 扩展 `gd`**(图形验证码 `mews/captcha`、intervention/image 依赖它生成图片;
   素材库 `getimagesize` 读尺寸也依赖它)—— 缺失时验证码不生成、图片尺寸读不到。
2. **缺 `fileinfo` 扩展** —— 上传校验(Laravel `validate('image')` / MIME 检测)失败,
   常见报错 `ext-fileinfo is missing`。
3. **`curl_exec` 被禁用**(宝塔「安全防护」插件常禁)—— 支付宝/微信等原生驱动发起支付、
   货源对接(上游 HTTP)、远程图片抓取全部失败。
4. **`putenv` / `proc_open` 被禁用** —— 见「宝塔禁用函数」表与问题 4 / 问题 8。

**排查**

```bash
# ① 确认扩展(应包含 gd / fileinfo / curl / mbstring / bcmath)
/www/server/php/84/bin/php -m | grep -E "^(gd|fileinfo|curl|mbstring|bcmath)$"

# ② 确认关键函数可用(应输出 bool(true))
/www/server/php/84/bin/php -r "var_dump(function_exists('curl_exec'));"
/www/server/php/84/bin/php -r "var_dump(function_exists('getimagesize'));"
/www/server/php/84/bin/php -r "var_dump(function_exists('putenv'));"
/www/server/php/84/bin/php -r "var_dump(function_exists('proc_open'));"

# ③ 查看当前禁用列表(为空 = 未禁用)
grep -i "disable_functions" /www/server/php/84/etc/php.ini /www/server/php/84/etc/php-cli.ini
```

**解决**

1. **装扩展**:宝塔 → PHP-8.4 → 设置 → 安装扩展 → 安装 `gd`(或 `imagick`)、`fileinfo`。
2. **放行函数**:PHP 设置 → 禁用函数,删除 `curl_exec`、`putenv`、`proc_open`;
   若装过宝塔「安全防护/防篡改」插件,在其配置中放行上述函数。
3. 重载 PHP-FPM 后重试:
   ```bash
   /etc/init.d/php-fpm-84 reload
   ```

> 区分口诀:**图片/验证码问题先查扩展(gd/fileinfo),支付/货源/远程问题先查禁用函数(curl_exec)**。

---

### 问题 15:更新后后台/前台空白页(Cloudflare Rocket Loader)

**现象**:站点在 Cloudflare 之后(开启 **Rocket Loader**),更新后打开 `/admin`
页面空白(标题/URL 正常,`#app` 为空注释),API 与静态资源均 200。

**原因**:Cloudflare **Rocket Loader** 会改写页面 `<script>` 标签(异步延迟加载),
对 ES Module 脚本处理不当导致 Vue 应用未执行 —— SPA 空白且无报错。

**解决**(v1.12.55 起产物已内置修复,无需额外配置):
- 编译后的 `index.html` 已为 module 入口加 **`data-cfasync="false"`**,
  跳过 Rocket Loader 处理。更新到 v1.12.55+ 并**强刷缓存**即可。
- 或手动在 Cloudflare → Speed → Optimization → Rocket Loader **关闭**。

**验证**:更新后 `curl -s 你的域名/admin | grep data-cfasync` 应输出
`<script type="module" data-cfasync="false" ...>`。

---

### 问题 16:更新后接口报 `Unknown column ... in field list`(数据库迁移未执行)

**现象**

代码或后台显示已经更新到新版本,但下单、支付、商品保存等接口报错:

```text
SQLSTATE[42S22]: Column not found: 1054 Unknown column
'fulfillment_type_snapshot' in 'field list'
```

实际字段名可能不同,但共同特征是 `Unknown column '字段名' in 'field list'`。

**原因**

代码已经使用新字段,而当前站点连接的数据库还没有执行对应 Laravel 迁移。
常见原因包括:

1. 只执行了 `git pull` / `git reset`,没有执行 `php artisan migrate --force`;
2. 在线更新过程中迁移失败,但运维只核对了代码版本,没有核对迁移状态和更新日志;
3. 在错误的项目目录、PHP 容器或 `.env` 环境中执行了迁移,实际业务数据库仍未升级;
4. 迁移记录和真实表结构不一致(迁移显示 `Ran`,字段却不存在)。

> 版本号更新只证明代码文件已同步,不证明数据库迁移已经成功。

**标准处理步骤**

必须进入**实际运行站点的项目根目录和 PHP 容器/环境**,并确认当前 `.env`
连接的就是报错中的数据库:

```bash
cd <实际项目目录>

# 1. 查看迁移状态;Pending 表示尚未执行
php artisan migrate:status

# 2. 执行所有待处理迁移
php artisan migrate --force

# 3. 清除旧配置、路由、视图和应用缓存
php artisan optimize:clear

# 4. 再次确认不应存在 Pending
php artisan migrate:status
```

如错误涉及履约字段,可进一步验证真实表结构:

```bash
php artisan tinker --execute="dump(
    Schema::hasColumn('orders', 'fulfillment_type_snapshot'),
    Schema::hasColumn('orders', 'delivery_message_snapshot'),
    Schema::hasColumn('products', 'fulfillment_type')
);"
```

对应字段存在时应全部输出 `true`。完成后重新创建一笔测试订单,验证下单、支付和发货链路。

**迁移显示 `Ran`,但字段仍不存在时**

不要直接删除 `migrations` 表记录或盲目重跑旧迁移。旧迁移可能不是幂等的,
部分字段已经存在时重跑会触发 `Duplicate column` 并造成新的中断。正确处理方式是:

1. 先备份数据库;
2. 对照迁移文件检查相关表的实际字段;
3. 新增一个使用 `Schema::hasColumn()` 判断的幂等修复迁移;
4. 执行修复迁移后再次核对字段和业务链路。

在线更新发生过异常时,同时查看 `storage/app/update.log` 中的 Git、Composer 和迁移输出。
如果命令伴随 `Module "mbstring" is already loaded`,它只是扩展重复加载警告,
不影响迁移执行,可按[问题 7](#问题-7module-mbstring-is-already-loaded)单独清理。

---

## 错误速查表

| 报错关键词 | 根因 | 解决 |
|---|---|---|
| `composer-runtime-api ^2.2 ... found 2.0.0` | Composer 版本太旧 | 升级 Composer ≥ 2.8 |
| 满屏 `Implicitly marking parameter ... nullable is deprecated` | Composer phar 在新 PHP 下过时 | 升级 Composer |
| `Failed to open stream: vendor/autoload.php` | 依赖没装上 | 先解决其它 install 失败问题 |
| `ext-fileinfo is missing` | 缺 fileinfo 扩展 | 宝塔扩展页装 fileinfo |
| `ext-gd is missing` / 验证码空白 / 图片尺寸为空 | 缺 gd 扩展 | 宝塔扩展页装 gd(验证码/素材库尺寸依赖) |
| 发起支付报「网关请求失败」/ 货源同步失败 | `curl_exec` 被禁用(安全插件) | PHP 设置→禁用函数 移除 curl_exec;安全插件放行 |
| 上传图片失败 / 验证码不显示 | 见问题 14 | 先装 gd/fileinfo 扩展,再放行禁用函数 |
| `ext-bcmath is missing` | 缺 bcmath 扩展 | 宝塔扩展页装 bcmath(金额计算必需) |
| `Call to undefined function putenv()` | putenv 被禁用 | 禁用函数列表移除 putenv |
| `Call to undefined function proc_open()` | proc_open 被禁用 | 禁用函数列表移除 proc_open |
| **访问首页 500 / 白屏(CLI 正常)** | **storage 目录属主 root,PHP-FPM www 写不了** | **`chown -R www:www storage bootstrap/cache`(问题 5)** |
| **`tempnam(): file created in system temp`** | **open_basedir 路径限制** | **放宽 open_basedir 到项目父目录 + /tmp(问题 6)** |
| **`Module "mbstring" already loaded`** | **mbstring 重复加载** | **删除 php.ini 重复的 extension 行(问题 7)** |
| **`The route ... could not be found`** | **线上代码旧(git pull 冲突/未更新)或 OPcache 缓存旧代码** | **`git reset --hard origin/main` + `route:clear` + 重启 PHP-FPM(问题 8)** |
| **货源接口 `supply-sources/{id}/products/preview` 404** | **代码 < v1.9.2 或货源 id 不存在** | **更新代码到 v1.9.2+ / 用 tinker 查可用 id(问题 8)** |
| **接口报错返回 HTML 404 页而非 JSON** | **`error_page 404 /404.html` 吞掉 JSON** | **注释 nginx 的 error_page 404(问题 9)** |
| **`/storage/...` 图片 403「访问被拒绝」不显示** | **`public/storage` 软链接缺失或指向错误** | **`php artisan storage:link` 重建链接(问题 10)** |
| **`Your lock file does not contain a compatible set of packages` / `symfony/yaml v8.x requires php >=8.4`** | **服务器 composer.lock 旧 / Symfony 锁到 8.x 不兼容 PHP 8.3** | **`git reset --hard origin/main` 同步 lock + `composer install`(问题 11)** |
| **`yansongda/pay ... requires ext-bcmath`** | **服务器 PHP 缺 bcmath 扩展** | **安装 bcmath 扩展(宝塔扩展页 / docker-php-ext-install bcmath)(问题 11)** |
| **`/tmp/.composer/config.json is not readable`** | **Composer 全局配置目录权限冲突** | **删 `/tmp/.composer` 或设 `COMPOSER_HOME` 到项目内(问题 12)** |
| **`SQLSTATE[42S22]` / `Unknown column ... in field list`** | **代码已更新,实际业务数据库仍有 Pending 迁移或迁移记录与表结构不一致** | **在实际项目/PHP 环境执行 `migrate:status`、`migrate --force`、`optimize:clear`(问题 16)** |
| `Do not run Composer as root/super user!` | 用 root 跑 Composer | 仅警告,可输 `yes` 继续;但装完记得 `chown -R www:www vendor` |
| 静态资源 404 / 后台白屏 | Web 根目录没指向 `public/` | Nginx `root` 改为 `.../ZCard/public` |
| 安装向导页面白屏(JS 404) | storefront vite base 路径(v1.1.2 已修) | 升级到 v1.1.3+ |
| 直接 `php artisan` 用错 PHP 版本 | 系统 PATH 里的 php 不是 8.4 | `ln -sf /www/server/php/84/bin/php /usr/local/bin/php` |

---

## 部署后检查清单

```bash
cd <你的项目目录>

# ⚠️ 最重要:权限属主(root 跑 composer/git pull 后必须执行,否则 Web 必 500)
chown -R www:www storage bootstrap/cache vendor
chmod -R 775 storage bootstrap/cache
# 验证 www 用户可写:
sudo -u www test -w storage && echo "✅ storage 可写" || echo "❌ 不可写"

# 1. PHP 版本与扩展
php -v                                           # 8.3 / 8.4
php -m | grep -iE 'fileinfo|bcmath|gd|pdo_mysql|curl|mbstring|openssl'

# 2. 禁用函数已放行
php -r "var_dump(function_exists('putenv'));"    # bool(true)

# 3. 依赖已装
ls vendor/autoload.php                           # 存在

# 4. 关键目录存在且可写
ls -d storage bootstrap/cache

# 5. 环境配置
test -f .env && echo ".env OK"
php artisan key:generate --check 2>/dev/null || php artisan key:generate

# 6. 数据库连接与迁移状态(版本号更新不代表迁移已完成)
php artisan migrate:status
php artisan migrate --force
php artisan migrate:status                         # 再次确认无 Pending

# 7. storage 软链接(素材/上传图片经 /storage/ 访问必需,v1.11.11+ 安装自动建)
ls -la public/storage
# 期望:public/storage -> ../storage/app/public
# 缺失或指向错误时执行:php artisan storage:link

# 8. 缓存清一遍 + 重启 PHP-FPM
php artisan optimize:clear
/etc/init.d/php-fpm-84 restart
```

逐项通过后,访问 `http://你的域名/`(前台)和 `http://你的域名/admin/`(后台)即可。
再验证图片访问:`curl -I http://你的域名/storage/media/...` 应返回 200。

> **首次部署捷径**:clone + `composer install` 后,直接访问首页会自动跳转 `/install` 安装向导(v1.1.2+),按向导填写数据库和管理员信息即可完成安装。安装向导会自动处理目录权限和 APP_KEY 生成。

---

*如安装中遇到本文档未覆盖的问题,欢迎在 Telegram 群 [@ZhonCard](https://t.me/ZhonCard) 反馈。*
