# v1.9.19

## 🔧 本次更新内容

### dujiao-next(独角数卡)上游对接复查修复

对照 dujiao-next 参考源码逐项复查，修复 5 个协议问题：

| # | 问题 | 修复 |
|---|---|---|
| 1 | ping 站点名读取字段错误 | 改读 `site_name` |
| 2 | **下单参数错误**：上游按 `sku_id` 下单（非商品 id），导致必然「sku not found」 | 下单前拉商品详情，取第一个启用 SKU 的 id 作为 `sku_id` |
| 3 | 库存/价格在 SKU 层未解析（库存恒为 -1） | 解析并填充 SKU 列表 + 库存 |
| 4 | **回调验签恒失败**：上游签名 path 固定为 `/api/v1/upstream/callback` | 验签改用固定 path |
| 5 | 批发价字段结构解析错误（拿货价错） | 改读 `wholesale_prices[0].unit_price` |

新增 `DujiaoNextDriverProtocolTest` 回归测试（4 用例）。

### 检查结论

- **acg-faka**：签名/路径/字段与参考源码一致，无问题（此前已修复查单路径/订单号/多卡拆分/金额）
- **ZCard 自对接**：驱动与本站 `SupplyAuth` 共用同一签名工具、字段一一对应，无问题

## ⬆️ 升级

> 在线更新：后台 → 在线更新 → 检查版本 → 一键更新即可（Git-based）。

```bash
# 手动方式
git pull origin main
php artisan route:clear && php artisan config:clear
```

**无数据库迁移**。

## 修复清单

- dujiao-next：sku 下单 / SKU 库存 / 回调验签 path / ping 站点名 / 批发价字段
