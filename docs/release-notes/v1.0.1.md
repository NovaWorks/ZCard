## ✨ 新增
- **Web 安装向导**: 浏览器可视化安装(环境检测 → 数据库配置 → 管理员创建 → 完成),无需命令行
- **命令行交互式安装**: `php artisan zcard:install` 交互式引导
- **在线更新页全面重设计**: 大厂风格 UI,检查更新/版本历史/立即更新/版本回退
- **版本回退功能**: 一键 `git reset HEAD~1` 回退到上一版本(含前端重建)

## 🐛 修复
- 安装向导 3 个 BLOCKER + 4 个 HIGH/MEDIUM 问题修复
- CLI 安装 `username=admin` 重复冲突
- `UpdateController::check()` 回退支持 prerelease Release
- 前端编译产物提交到 git(`public/admin` + `public/storefront`),生产部署无需 Node.js
- `.gitignore` 排除 `public/storefront/` 编译产物冲突

## 📚 文档
- 重写 README(安装方式/环境要求/功能总览/版本规则/路线图)
- 补充 Web 服务器配置(Nginx/Apache/宝塔 + 伪静态规则)
