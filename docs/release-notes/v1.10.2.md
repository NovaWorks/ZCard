# v1.10.2

## 🧹 优化:货源管理页面代码质量

修复货源管理页面(`sysadmin/src/views/supply/list/index.vue`)全部 ESLint 告警(76 处):

- **Prettier 格式化(69 处)**:模板属性多行化、逗号/换行规范,统一代码风格
- **未使用变量清理(7 处)**:
  - 6 处 `catch (e)` 改为 `catch {}`(错误处理依赖 HTTP 拦截器,无需 e)
  - 删除未使用的 `setCategoryMapping` 函数
- **无任何功能变更**,页面功能回归验证通过(货源列表/拉取商品/全选新货源等均正常)

## ⬆️ 升级

在线更新:后台 → 在线更新 → 一键更新。**无数据库迁移**,`public/admin` 编译产物已随仓库更新。
